{
  "main": "lib/index.js",
  "version": "2.0.2",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "release-it": "yarn version --patch && git push --follow-tags"
  },
  "dependencies": {
    "emittery": "^0.6.0",
    "henta": "^3.0.0",
    "sequelize": "^5.21.3"
  }
}
